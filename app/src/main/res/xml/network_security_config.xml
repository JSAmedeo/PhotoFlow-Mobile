<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permit cleartext HTTP globally — backend URL is user-configured and LAN IPs vary -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
